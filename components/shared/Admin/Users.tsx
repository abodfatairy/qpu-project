import { GetAllUsers } from "@/data/data";
import React from "react";

const Users = async () => {
  return (
    <div className=' bg-red-50 w-screen p-5'>
      {/* {data.map((item: any) => {
        return (
          <div
            className=''
            key={item.name}
          >
            {item.name}
          </div>
        );
      })} */}
    </div>
  );
};

export default Users;
